# 内嵌 frp 实现计划（C+B 方案）

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 把 frp 从"外部二进制调用"改为"Go 库内嵌"。客户端 import `github.com/fatedier/frp/client` 在进程内跑 frpc（零外部依赖）；服务端 agent import `github.com/fatedier/frp/server` 在进程内跑 frps（agent 真正掌控 frps 生命周期，不再需要独立 frps 二进制）。配置直接构造 frp 官方 `v1.*Configurer` 对象，不走 TOML 文本往返。

**架构：**
- 客户端 `internal/frpc`：`Manager` 从 `exec.Cmd` 改成 `*client.Service`，配置从自写 `Config` 改成 `v1.ClientCommonConfig` + `[]v1.ProxyConfigurer`，传给 `client.NewService` + `svr.Run(ctx)`。`Start/Stop/Restart` 通过 ctx cancel 控制 Service 生命周期。
- 服务端 `internal/frps`：`Manager` 从进程检测改成内嵌 `server.NewService(cfg)`，`Start` 真正启动 frps goroutine，`Stop` 通过 ctx cancel，`Status` 通过内嵌 service 状态判断。`Config()` 仍解析 frps.toml 但改用 frp 官方 `config.LoadServerConfig`。
- 两端 `go.mod`：`require github.com/fatedier/frp v0.69.1`，删除自写的 `frpsc`/`frpc` 配置结构（用官方类型替代）。
- 前端 + 业务逻辑不变：`App` 方法签名对外不变，内部适配新 Manager。

**技术栈：** `github.com/fatedier/frp v0.69.1`（client + server + pkg/config/v1）、Go 1.26、现有 chi/sqlite/wails 不变。

**关键 API（已调研确认）：**
- frpc：`client.NewService(client.ServiceOptions{Common: *v1.ClientCommonConfig, ConfigSourceAggregator: *source.Aggregator, UnsafeFeatures: ...})` → `(*Service, error)`；`svr.Run(ctx)` 阻塞；ctx cancel 触发停止。
- 配置源：`source.NewConfigSource()` + `configSource.ReplaceAll(proxyCfgs, visitorCfgs)` → `source.NewAggregator(configSource)`。
- frps：`server.NewService(cfg *v1.ServerConfig)` → `(*Service, error)`；`svr.Run(ctx)`。
- 配置加载：`config.LoadServerConfig(path, strict)` → `(*v1.ServerConfig, isLegacy, error)`；`config.LoadClientConfig(path, strict)` → `(*v1.ClientCommonConfig, []v1.ProxyConfigurer, []v1.VisitorConfigurer, error)`。
- 构造 Proxy：`v1.TCPProxyConfig{ProxyBaseConfig: v1.ProxyBaseConfig{Name, Type, ProxyBackend: v1.ProxyBackend{LocalIP, LocalPort}}, RemotePort}`；HTTP/HTTPS 用 `DomainConfig{CustomDomains, SubDomain}`。
- 构造 ClientCommon：`v1.ClientCommonConfig{ServerAddr, ServerPort, Auth: v1.AuthConfig{Token}}`。
- 构造 ServerConfig：`v1.ServerConfig{BindPort, VhostHTTPPort, VhostHTTPSPort, SubDomainHost, Auth, AllowPorts}`。

**约定：**
- `v1.*` 指 `github.com/fatedier/frp/pkg/config/v1`。
- frp 用 `//go:build !frps` 标签隔离 client 包代码——import client 包时**不能**带 `-tags frps`，正常编译即可。
- ` UnsafeFeatures`：v0.69 用 `security.NewUnsafeFeatures(allowUnsafe)`，我们传空切片 `security.NewUnsafeFeatures(nil)`。
- 日志：frp 用自己的 `pkg/util/log`，内嵌后其日志默认输出到 stdout，我们已用 Wails 事件转发；服务端 agent 用 chi 的 Logger middleware 不冲突。
- `v1.ServerConfig.AllowPorts` 类型是 `[]v1.PortRange`，每个 `{Start, End}`。

**测试策略：** frpc 内嵌后单测不再用 ping/sleep 替身，改用 `httptest` 起假 frps 验证 Service 能连（或直接验证配置构造正确性，不连真实 frps）。frps 内嵌后单测验证 `NewService` 不报错 + `Run` 能监听端口。每步用 `go build && go vet && go test` 验证。

---

## 文件结构

**客户端修改：**
- `client/internal/frpc/config.go` — 删除自写 `Config`/`Auth`/`Proxy`/`Generate`，改用 `v1.*` 类型 + 构造辅助函数
- `client/internal/frpc/manager.go` — `Manager` 从 `exec.Cmd` 改成 `*client.Service` + ctx cancel
- `client/internal/frpc/frpc.go` — `Manager` 接口签名适配（Start/Stop/Restart 用 v1 配置对象）
- `client/internal/frpc/config_test.go` — 改测试为验证 v1 配置构造
- `client/internal/frpc/manager_test.go` — 改测试为验证 Service 生命周期（用假 frps 或只测配置）
- `client/app.go` — `GenerateFrpcConfig`/`StartFrpc` 等适配新 Manager 签名；`GenerateFrpcConfig` 仍返回 TOML 文本（用于"查看配置"功能），但内部用官方 `v1` 类型构造后用 toml 序列化
- `client/app_test.go` — 适配
- `client/go.mod` — require `github.com/fatedier/frp v0.69.1`

**服务端修改：**
- `server/internal/frps/manager.go` — 内嵌 `server.NewService`，Start/Stop/Restart 真正实现
- `server/internal/frps/frps.go` — `Manager` 接口适配（Config 返回 `*v1.ServerConfig`）
- `server/internal/frps/manager_test.go` — 适配
- `server/internal/frpsc/` — 删除整个包（用官方 `config.LoadServerConfig` 替代）
- `server/internal/api/api.go` — `capabilities` handler 改用 `*v1.ServerConfig` 字段
- `server/internal/api/api_test.go` — 适配
- `server/internal/portpool/portpool.go` + `manager.go` — `IsPortAllowed` 改用 `v1.ServerConfig.AllowPorts`
- `server/internal/portpool/manager_test.go` — 适配
- `server/cmd/agent/main.go` — 装配内嵌 frps（agent 启动时按配置启动 frps goroutine）
- `server/go.mod` — require `github.com/fatedier/frp v0.69.1`

**删除：**
- `server/internal/frpsc/` 整个包（config.go + config_test.go）

---

### 任务 1：两端 go.mod 引入 frp 依赖

**文件：**
- 修改：`server/go.mod`、`client/go.mod`

- [ ] **步骤 1：server 引入 frp**

在 `server/` 目录执行：

```bash
go get github.com/fatedier/frp@v0.69.1
```

若网络失败，`$env:GOPROXY="https://goproxy.cn,direct"` 后重试。这会下载 frp 及其全部依赖（yamux/quic/kcp 等，体积较大，首次较慢）。

- [ ] **步骤 2：client 引入 frp**

在 `client/` 目录执行：

```bash
go get github.com/fatedier/frp@v0.69.1
```

- [ ] **步骤 3：验证依赖就位**

两端分别 `go build ./...`，确认无编译错误（此时还没改代码，应仍能编译）。

- [ ] **步骤 4：Commit**

```bash
git add server/go.mod server/go.sum client/go.mod client/go.sum
git commit -m "chore: 两端引入 github.com/fatedier/frp v0.69.1 依赖"
```

---

### 任务 2：服务端删除 frpsc 包，改用官方 config.LoadServerConfig

**文件：**
- 删除：`server/internal/frpsc/config.go`、`server/internal/frpsc/config_test.go`
- 修改：`server/internal/frps/frps.go`、`server/internal/frps/manager.go`、`server/internal/frps/manager_test.go`
- 修改：`server/internal/api/api.go`、`server/internal/api/api_test.go`
- 修改：`server/internal/portpool/portpool.go`、`server/internal/portpool/manager.go`、`server/internal/portpool/manager_test.go`

frpsc 包原来自己解析 frps.toml，现在改用 frp 官方 `config.LoadServerConfig(path, true)`，返回 `*v1.ServerConfig`。`v1.ServerConfig` 字段：`BindPort int`、`VhostHTTPPort int`、`VhostHTTPSPort int`、`SubDomainHost string`、`Auth v1.AuthConfig{Token}`、`AllowPorts []v1.PortRange{Start,End}`。注意官方字段是 `int` 非 `*int`，未设置时为 0——capabilities 判断 `SupportHTTP` 改成 `VhostHTTPPort != 0`。

- [ ] **步骤 1：删除 frpsc 包**

```bash
Remove-Item -Recurse -Force server/internal/frpsc
```

- [ ] **步骤 2：重写 frps/frps.go（接口）**

`server/internal/frps/frps.go`：

```go
// Package frps 内嵌 frps 服务并管理其生命周期。
package frps

import (
	"context"

	v1 "github.com/fatedier/frp/pkg/config/v1"
)

// Status 描述 frps 当前状态。
type Status struct {
	Running  bool   `json:"running"`
	Version  string `json:"version,omitempty"`
	BindPort int    `json:"bind_port,omitempty"`
	Error    string `json:"error,omitempty"`
}

// Manager 抽象 frps 的控制能力（由 manager.go 实现）。
type Manager interface {
	Status(ctx context.Context) (*Status, error)
	Config() (*v1.ServerConfig, error)
	ConfigPath() string
	Start(ctx context.Context) error
	Stop(ctx context.Context) error
	Restart(ctx context.Context) error
}
```

- [ ] **步骤 3：重写 frps/manager.go（内嵌 server.NewService）**

`server/internal/frps/manager.go`：

```go
package frps

import (
	"context"
	"fmt"
	"net"
	"strconv"
	"sync"
	"time"

	"github.com/fatedier/frp/pkg/config"
	"github.com/fatedier/frp/pkg/util/version"
	"github.com/fatedier/frp/server"
	v1 "github.com/fatedier/frp/pkg/config/v1"
)

// Manager 内嵌 frps 服务。
type Manager struct {
	mu       sync.Mutex
	cfgPath  string
	cancel   context.CancelFunc
	running  bool
	bindPort int
}

// NewManager 创建 frps 管理器。
func NewManager(cfgPath string) *Manager {
	return &Manager{cfgPath: cfgPath}
}

// Config 加载并返回 frps 配置（官方 v1.ServerConfig）。
func (m *Manager) Config() (*v1.ServerConfig, error) {
	cfg, _, err := config.LoadServerConfig(m.cfgPath, true)
	if err != nil {
		return nil, fmt.Errorf("加载 frps 配置 %s: %w", m.cfgPath, err)
	}
	return cfg, nil
}

// ConfigPath 返回 frps.toml 路径。
func (m *Manager) ConfigPath() string { return m.cfgPath }

// Status 检测 frps 是否运行。
func (m *Manager) Status(_ context.Context) (*Status, error) {
	cfg, err := m.Config()
	if err != nil {
		return nil, err
	}
	m.mu.Lock()
	running := m.running
	m.mu.Unlock()
	// 若内嵌未启动，回退到端口探测
	if !running {
		running = isPortListening(cfg.BindPort)
	}
	return &Status{
		Running:  running,
		Version:  version.Full(),
		BindPort: cfg.BindPort,
	}, nil
}

// Start 内嵌启动 frps。
func (m *Manager) Start(ctx context.Context) error {
	m.mu.Lock()
	defer m.mu.Unlock()
	if m.running {
		return fmt.Errorf("frps 已在运行")
	}
	cfg, err := m.Config()
	if err != nil {
		return err
	}
	svr, err := server.NewService(cfg)
	if err != nil {
		return fmt.Errorf("创建 frps service: %w", err)
	}
	runCtx, cancel := context.WithCancel(ctx)
	m.cancel = cancel
	m.running = true
	m.bindPort = cfg.BindPort
	go func() {
		_ = svr.Run(runCtx)
		m.mu.Lock()
		m.running = false
		m.mu.Unlock()
	}()
	// 给 frps 一点启动时间绑定端口
	time.Sleep(200 * time.Millisecond)
	return nil
}

// Stop 停止内嵌 frps。
func (m *Manager) Stop(_ context.Context) error {
	m.mu.Lock()
	defer m.mu.Unlock()
	if !m.running || m.cancel == nil {
		return fmt.Errorf("frps 未运行")
	}
	m.cancel()
	m.cancel = nil
	m.running = false
	return nil
}

// Restart 重启 frps。
func (m *Manager) Restart(ctx context.Context) error {
	_ = m.Stop(ctx)
	return m.Start(ctx)
}

// isPortListening 判断本地 TCP 端口是否在监听。
func isPortListening(port int) bool {
	conn, err := net.DialTimeout("tcp", "127.0.0.1:"+strconv.Itoa(port), 300*time.Millisecond)
	if err != nil {
		return false
	}
	_ = conn.Close()
	return true
}
```

> 注意：`server.NewService` 内部会启动监听，`Run(ctx)` 阻塞直到 ctx cancel。`Start` 里用 goroutine 跑 `Run`，ctx cancel 即停止。`version.Full()` 返回 frp 版本字符串。

- [ ] **步骤 4：重写 frps/manager_test.go**

```go
package frps

import (
	"context"
	"os"
	"path/filepath"
	"testing"
)

func writeFrpsToml(t *testing.T, body string) string {
	t.Helper()
	p := filepath.Join(t.TempDir(), "frps.toml")
	if err := os.WriteFile(p, []byte(body), 0o644); err != nil {
		t.Fatal(err)
	}
	return p
}

func TestConfig(t *testing.T) {
	p := writeFrpsToml(t, `bindPort = 7000
vhostHTTPPort = 80
vhostHTTPSPort = 443
subDomainHost = "frp.example.com"

[auth]
token = "tok"

[[allowPorts]]
start = 10000
end = 60000
`)
	m := NewManager(p)
	cfg, err := m.Config()
	if err != nil {
		t.Fatalf("Config: %v", err)
	}
	if cfg.BindPort != 7000 {
		t.Errorf("BindPort = %d", cfg.BindPort)
	}
	if cfg.VhostHTTPPort != 80 {
		t.Errorf("VhostHTTPPort = %d", cfg.VhostHTTPPort)
	}
	if cfg.SubDomainHost != "frp.example.com" {
		t.Errorf("SubDomainHost = %q", cfg.SubDomainHost)
	}
	if cfg.Auth.Token != "tok" {
		t.Errorf("Auth.Token = %q", cfg.Auth.Token)
	}
	if len(cfg.AllowPorts) != 1 || cfg.AllowPorts[0].Start != 10000 || cfg.AllowPorts[0].End != 60000 {
		t.Errorf("AllowPorts = %+v", cfg.AllowPorts)
	}
}

func TestStatus_NotRunning(t *testing.T) {
	p := writeFrpsToml(t, `bindPort = 7000`)
	m := NewManager(p)
	st, err := m.Status(context.Background())
	if err != nil {
		t.Fatalf("Status: %v", err)
	}
	if st.Running {
		t.Errorf("Running = true, want false（无真实 frps 监听 7000）")
	}
	if st.BindPort != 7000 {
		t.Errorf("BindPort = %d", st.BindPort)
	}
}

func TestConfig_MissingFile(t *testing.T) {
	m := NewManager(filepath.Join(t.TempDir(), "nope.toml"))
	if _, err := m.Config(); err == nil {
		t.Fatal("缺失配置应返回 error")
	}
}
```

- [ ] **步骤 5：更新 api.go capabilities 用 v1.ServerConfig**

`server/internal/api/api.go` 的 `capabilities` handler 改用 `*v1.ServerConfig`：

```go
func (s *Server) capabilities(w http.ResponseWriter, _ *http.Request) {
	cfg, err := s.frps.Config()
	if err != nil {
		writeJSON(w, http.StatusInternalServerError, map[string]string{"error": "读取 frps 配置失败: " + err.Error()})
		return
	}
	st, _ := s.frps.Status(nil)
	resp := CapabilitiesResponse{
		BindPort:           cfg.BindPort,
		FrpsRunning:        st != nil && st.Running,
		FrpsVersion:        st.Version,
		SupportTCP:         true,
		SupportUDP:         true,
		SupportHTTP:        cfg.VhostHTTPPort != 0,
		SupportHTTPS:       cfg.VhostHTTPSPort != 0,
		VhostHTTPPort:      cfg.VhostHTTPPort,
		VhostHTTPSPort:     cfg.VhostHTTPSPort,
		SubdomainHost:      cfg.SubDomainHost,
		AllowedRootDomains: s.cfg.Domain.AllowedRootDomains,
	}
	for _, pr := range cfg.AllowPorts {
		resp.AllowPorts = append(resp.AllowPorts, AllowPortRange{Start: pr.Start, End: pr.End})
	}
	writeJSON(w, http.StatusOK, resp)
}
```

api.go import 块把 `github.com/kdc/frp-manager/server/internal/frpsc` 删除，加 `v1 "github.com/fatedier/frp/pkg/config/v1"`（若类型引用需要）。

> 注意：`frps.Manager` 接口的 `Config()` 现在返回 `*v1.ServerConfig`，api.Server 的 `frps` 字段类型是 `frps.Manager`（接口）。`s.frps.Config()` 返回 `*v1.ServerConfig`，字段访问 `cfg.VhostHTTPPort` 等。`AllowPortRange`（api 包自定义的响应结构）保留不变，转换 `pr.Start/pr.End`。

- [ ] **步骤 6：更新 portpool 用 v1.ServerConfig.AllowPorts**

`server/internal/portpool/portpool.go` 不变（Protocol/CheckResult 类型）。

`server/internal/portpool/manager.go` 把 `frpCfg *frpsc.Config` 改成 `frpCfg *v1.ServerConfig`：

```go
import (
	// ...删除 frpsc
	v1 "github.com/fatedier/frp/pkg/config/v1"
)

type Manager struct {
	store  *store.Store
	frpCfg *v1.ServerConfig
}

func NewManager(s *store.Store, frpCfg *v1.ServerConfig) *Manager {
	return &Manager{store: s, frpCfg: frpCfg}
}

// Check/Allocate/Release 逻辑不变，只改 IsPortAllowed 调用
```

`IsPortAllowed` 改成方法或包函数，接收 `*v1.ServerConfig`：

```go
func (m *Manager) isPortAllowed(port int) bool {
	if m.frpCfg == nil || len(m.frpCfg.AllowPorts) == 0 {
		return true
	}
	for _, pr := range m.frpCfg.AllowPorts {
		if pr.Start != 0 && pr.End != 0 && port >= pr.Start && port <= pr.End {
			return true
		}
	}
	return false
}
```

把原 `Check` 里 `m.frpCfg.IsPortAllowed(port)` 调用改成 `m.isPortAllowed(port)`。

`allowRanges()` 改用 `m.frpCfg.AllowPorts`（`v1.PortRange{Start, End}`，注意官方类型没有 `Single` 字段，单端口用 Start==End 表示）：

```go
func (m *Manager) allowRanges() []portRange {
	if m.frpCfg == nil || len(m.frpCfg.AllowPorts) == 0 {
		return []portRange{{0, 65535}}
	}
	var out []portRange
	for _, pr := range m.frpCfg.AllowPorts {
		if pr.Start != 0 && pr.End != 0 {
			out = append(out, portRange{pr.Start, pr.End})
		}
	}
	return out
}
```

- [ ] **步骤 7：更新 main.go 装配**

`server/cmd/agent/main.go`：`frps.NewManager` 签名变了（去掉了 binaryPath 参数，因为内嵌不需要二进制）：

```go
	frpsMgr := frps.NewManager(cfg.Frps.Config)
	frpCfg, err := frpsMgr.Config()
	if err != nil {
		log.Printf("警告: 解析 frps 配置失败: %v", err)
	}
```

> 注意：agent 启动时是否自动 Start frps？设计文档第 4 节要求 agent "启动/停止/重启 frps"。本任务**不自动启动**——agent 提供 API 让客户端控制（v0.3 增强），v0.1 内嵌只需保证 `Start/Stop/Restart` 真正可用。若你希望 agent 启动即跑 frps，在 main.go 加 `go frpsMgr.Start(context.Background())`，但需确保 frps.toml 配置完整。**本计划不自动启动，保持 agent 与 frps 启动解耦。**

- [ ] **步骤 8：编译 + 测试**

```bash
cd server
go build ./... && go vet ./...
go test ./... -v
```

预期：所有测试通过。frpsc 包已删除，引用它的地方都改成官方 config。

- [ ] **步骤 9：Commit**

```bash
git add server/
git commit -m "refactor(server): 内嵌 frps，删除 frpsc 包改用官方 config.LoadServerConfig"
```

---

### 任务 3：客户端内嵌 frpc

**文件：**
- 修改：`client/internal/frpc/config.go`、`client/internal/frpc/manager.go`、`client/internal/frpc/frpc.go`
- 修改：`client/internal/frpc/config_test.go`、`client/internal/frpc/manager_test.go`
- 修改：`client/app.go`、`client/app_test.go`

- [ ] **步骤 1：重写 frpc/config.go（用 v1 类型 + 构造辅助）**

`client/internal/frpc/config.go`：

```go
package frpc

import (
	"fmt"

	"github.com/pelletier/go-toml/v2"

	v1 "github.com/fatedier/frp/pkg/config/v1"
)

// BuildClientConfig 根据 server 信息构造 frpc 客户端配置对象。
func BuildClientConfig(serverAddr string, serverPort int, token string) *v1.ClientCommonConfig {
	cfg := &v1.ClientCommonConfig{
		ServerAddr: serverAddr,
		ServerPort: serverPort,
	}
	cfg.Auth.Method = "token"
	cfg.Auth.Token = token
	return cfg
}

// BuildProxy 根据 tunnel 信息构造一个 v1.ProxyConfigurer。
func BuildProxy(name, protocol, localIP string, localPort int, remotePort int, customDomain, subdomain string) (v1.ProxyConfigurer, error) {
	base := v1.ProxyBaseConfig{
		Name: name,
		Type: protocol,
		ProxyBackend: v1.ProxyBackend{
			LocalIP:   localIP,
			LocalPort: localPort,
		},
	}
	switch protocol {
	case "tcp":
		return &v1.TCPProxyConfig{ProxyBaseConfig: base, RemotePort: remotePort}, nil
	case "udp":
		return &v1.UDPProxyConfig{ProxyBaseConfig: base, RemotePort: remotePort}, nil
	case "http":
		c := &v1.HTTPProxyConfig{ProxyBaseConfig: base}
		if customDomain != "" {
			c.CustomDomains = []string{customDomain}
		}
		if subdomain != "" {
			c.SubDomain = subdomain
		}
		return c, nil
	case "https":
		c := &v1.HTTPSProxyConfig{ProxyBaseConfig: base}
		if customDomain != "" {
			c.CustomDomains = []string{customDomain}
		}
		if subdomain != "" {
			c.SubDomain = subdomain
		}
		return c, nil
	default:
		return nil, fmt.Errorf("不支持的协议 %s", protocol)
	}
}

// MarshalConfig 把 v1 配置序列化为 frpc.toml 文本（用于"查看配置"功能）。
func MarshalConfig(common *v1.ClientCommonConfig, proxies []v1.ProxyConfigurer) (string, error) {
	// 复用 frp 官方 TOML 序列化：构造 map 后用 toml 编码
	out := map[string]any{
		"serverAddr": common.ServerAddr,
		"serverPort": common.ServerPort,
		"auth":       map[string]string{"method": common.Auth.Method, "token": common.Auth.Token},
	}
	var proxiesArr []map[string]any
	for _, p := range proxies {
		proxiesArr = append(proxiesArr, proxyConfigurerToMap(p))
	}
	if len(proxiesArr) > 0 {
		out["proxies"] = proxiesArr
	}
	var buf bytes.Buffer
	enc := toml.NewEncoder(&buf)
	if err := enc.Encode(out); err != nil {
		return "", fmt.Errorf("序列化 frpc.toml: %w", err)
	}
	return buf.String(), nil
}
```

> 注意：`proxyConfigurerToMap` 辅助函数把各 `ProxyConfigurer` 转成 map（json tag 即 toml 字段名）。简化做法：用 `json.Marshal` 再 `toml.Unmarshal` 转换，因为 v1 类型的 json tag 与 toml 字段名一致（都是 camelCase）。实现：

```go
import (
	"bytes"
	"encoding/json"
)

func proxyConfigurerToMap(p v1.ProxyConfigurer) map[string]any {
	b, _ := json.Marshal(p)
	var m map[string]any
	_ = json.Unmarshal(b, &m)
	return m
}
```

需在 config.go 顶部 import 加 `"bytes"`、`"encoding/json"`。

- [ ] **步骤 2：重写 frpc/frpc.go（接口）**

`client/internal/frpc/frpc.go`：

```go
// Package frpc 内嵌 frpc 服务并管理其生命周期。
package frpc

import (
	"context"

	v1 "github.com/fatedier/frp/pkg/config/v1"
)

// Manager 抽象 frpc 的控制能力（由 manager.go 实现）。
type Manager interface {
	Start(ctx context.Context, serverID string, common *v1.ClientCommonConfig, proxies []v1.ProxyConfigurer) error
	Stop(ctx context.Context, serverID string) error
	Restart(ctx context.Context, serverID string, common *v1.ClientCommonConfig, proxies []v1.ProxyConfigurer) error
	IsRunning(serverID string) bool
}
```

> 注意：接口去掉 `Generate` 方法（生成 TOML 文本移到包级 `MarshalConfig` 函数，App 直接调用）。`Start/Restart` 签名改为接收 `v1` 配置对象而非 cfgText 字符串。

- [ ] **步骤 3：重写 frpc/manager.go（内嵌 client.NewService）**

`client/internal/frpc/manager.go`：

```go
package frpc

import (
	"context"
	"fmt"
	"sync"

	"github.com/fatedier/frp/client"
	"github.com/fatedier/frp/pkg/config/source"
	"github.com/fatedier/frp/pkg/policy/security"
	v1 "github.com/fatedier/frp/pkg/config/v1"
)

// Manager 内嵌 frpc 服务，每个 serverID 对应一个 Service + ctx cancel。
type Manager struct {
	mu       sync.Mutex
	services map[string]*client.Service
	cancels  map[string]context.CancelFunc
	logCb    func(serverID, line string)
}

// NewManager 创建 frpc 管理器。
func NewManager() *Manager {
	return &Manager{
		services: map[string]*client.Service{},
		cancels:  map[string]context.CancelFunc{},
	}
}

// SetLogCallback 设置日志回调（frpc 内嵌后日志通过 frp 的 log 包输出，
// 实际集成时需把 frp log 重定向到回调；v0.1 先用默认 stdout）。
func (m *Manager) SetLogCallback(cb func(serverID, line string)) {
	m.logCb = cb
}

// Start 为指定 server 内嵌启动 frpc。
func (m *Manager) Start(ctx context.Context, serverID string, common *v1.ClientCommonConfig, proxies []v1.ProxyConfigurer) error {
	m.mu.Lock()
	defer m.mu.Unlock()
	if _, ok := m.services[serverID]; ok {
		return fmt.Errorf("server %s 的 frpc 已在运行", serverID)
	}
	if err := common.Complete(); err != nil {
		return fmt.Errorf("完善 client config: %w", err)
	}
	configSource := source.NewConfigSource()
	if err := configSource.ReplaceAll(proxies, nil); err != nil {
		return fmt.Errorf("设置 config source: %w", err)
	}
	aggregator := source.NewAggregator(configSource)
	unsafe := security.NewUnsafeFeatures(nil)
	svr, err := client.NewService(client.ServiceOptions{
		Common:                 common,
		ConfigSourceAggregator: aggregator,
		UnsafeFeatures:         unsafe,
	})
	if err != nil {
		return fmt.Errorf("创建 frpc service: %w", err)
	}
	runCtx, cancel := context.WithCancel(ctx)
	m.services[serverID] = svr
	m.cancels[serverID] = cancel
	go func() {
		_ = svr.Run(runCtx)
		m.mu.Lock()
		delete(m.services, serverID)
		delete(m.cancels, serverID)
		m.mu.Unlock()
	}()
	return nil
}

// Stop 停止指定 server 的 frpc。
func (m *Manager) Stop(_ context.Context, serverID string) error {
	m.mu.Lock()
	defer m.mu.Unlock()
	cancel, ok := m.cancels[serverID]
	if !ok {
		return fmt.Errorf("server %s 未运行", serverID)
	}
	cancel()
	delete(m.services, serverID)
	delete(m.cancels, serverID)
	return nil
}

// Restart 重启指定 server 的 frpc。
func (m *Manager) Restart(ctx context.Context, serverID string, common *v1.ClientCommonConfig, proxies []v1.ProxyConfigurer) error {
	_ = m.Stop(ctx, serverID)
	return m.Start(ctx, serverID, common, proxies)
}

// IsRunning 返回指定 server 的 frpc 是否在运行。
func (m *Manager) IsRunning(serverID string) bool {
	m.mu.Lock()
	defer m.mu.Unlock()
	_, ok := m.services[serverID]
	return ok
}
```

> 注意：`NewManager()` 不再需要 `configDir` 参数（不写配置文件了）。`main.go` 调用处要适配。

- [ ] **步骤 4：重写 config_test.go**

`client/internal/frpc/config_test.go`：

```go
package frpc

import (
	"strings"
	"testing"

	v1 "github.com/fatedier/frp/pkg/config/v1"
)

func TestBuildClientConfig(t *testing.T) {
	cfg := BuildClientConfig("1.2.3.4", 7000, "tok")
	if cfg.ServerAddr != "1.2.3.4" || cfg.ServerPort != 7000 || cfg.Auth.Token != "tok" {
		t.Errorf("got %+v", cfg)
	}
	if cfg.Auth.Method != "token" {
		t.Errorf("Auth.Method = %q", cfg.Auth.Method)
	}
}

func TestBuildProxy_TCP(t *testing.T) {
	p, err := BuildProxy("rdp", "tcp", "127.0.0.1", 3389, 20389, "", "")
	if err != nil {
		t.Fatal(err)
	}
	tcp, ok := p.(*v1.TCPProxyConfig)
	if !ok {
		t.Fatalf("type = %T, want *TCPProxyConfig", p)
	}
	if tcp.Name != "rdp" || tcp.LocalPort != 3389 || tcp.RemotePort != 20389 {
		t.Errorf("got %+v", tcp)
	}
}

func TestBuildProxy_HTTP_CustomDomain(t *testing.T) {
	p, err := BuildProxy("web", "http", "127.0.0.1", 3000, 0, "app.example.com", "")
	if err != nil {
		t.Fatal(err)
	}
	http, ok := p.(*v1.HTTPProxyConfig)
	if !ok {
		t.Fatalf("type = %T", p)
	}
	if len(http.CustomDomains) != 1 || http.CustomDomains[0] != "app.example.com" {
		t.Errorf("CustomDomains = %+v", http.CustomDomains)
	}
}

func TestBuildProxy_HTTPS_Subdomain(t *testing.T) {
	p, err := BuildProxy("demo", "https", "127.0.0.1", 8443, 0, "", "demo")
	if err != nil {
		t.Fatal(err)
	}
	https, ok := p.(*v1.HTTPSProxyConfig)
	if !ok {
		t.Fatalf("type = %T", p)
	}
	if https.SubDomain != "demo" {
		t.Errorf("SubDomain = %q", https.SubDomain)
	}
}

func TestBuildProxy_Unsupported(t *testing.T) {
	if _, err := BuildProxy("x", "stcp", "127.0.0.1", 22, 0, "", ""); err == nil {
		t.Fatal("不支持的协议应报错")
	}
}

func TestMarshalConfig(t *testing.T) {
	common := BuildClientConfig("1.2.3.4", 7000, "tok")
	p, _ := BuildProxy("rdp", "tcp", "127.0.0.1", 3389, 20389, "", "")
	out, err := MarshalConfig(common, []v1.ProxyConfigurer{p})
	if err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(out, "1.2.3.4") {
		t.Errorf("缺少 serverAddr: %s", out)
	}
	if !strings.Contains(out, "20389") {
		t.Errorf("缺少 remotePort: %s", out)
	}
}
```

- [ ] **步骤 5：重写 manager_test.go**

进程管理测试不再用 ping/sleep 替身。改成验证 Service 创建逻辑（不连真实 frps）：

`client/internal/frpc/manager_test.go`：

```go
package frpc

import (
	"context"
	"testing"

	v1 "github.com/fatedier/frp/pkg/config/v1"
)

func TestStartStop_NotConnected(t *testing.T) {
	// 指向一个不存在的 server，frpc 会 login 失败但 Service 会被创建
	// 验证 Start 不报错 + IsRunning=true + Stop 清理
	m := NewManager()
	common := BuildClientConfig("127.0.0.1", 1, "tok") // port 1 不可达
	p, _ := BuildProxy("test", "tcp", "127.0.0.1", 9999, 20000, "", "")
	err := m.Start(context.Background(), "s1", common, []v1.ProxyConfigurer{p})
	if err != nil {
		// login 失败可能导致 Start 返回 error，取决于 loginFailExit 设置
		// 这里允许 err 非 nil，只要不 panic
		t.Logf("Start 返回（预期 login 失败）: %v", err)
	}
	// Stop 应能清理（即使 Start 失败）
	_ = m.Stop(context.Background(), "s1")
	if m.IsRunning("s1") {
		t.Errorf("Stop 后不应 running")
	}
}

func TestStopNotRunning(t *testing.T) {
	m := NewManager()
	if err := m.Stop(context.Background(), "s1"); err == nil {
		t.Error("停止未运行的应报错")
	}
}

func TestIsRunning_False(t *testing.T) {
	m := NewManager()
	if m.IsRunning("s1") {
		t.Error("未启动应 false")
	}
}
```

> 注意：frpc `Service.Run` 在 login 失败时行为取决于 `LoginFailExit`（默认 true 会退出，false 会重试）。我们的 `BuildClientConfig` 没设 `LoginFailExit`，用默认值。测试用 port 1（不可达），预期 login 失败。测试主要验证不 panic + Stop 能清理，不强求 Start 成功。

- [ ] **步骤 6：更新 app.go 适配新 Manager 签名**

`client/app.go` 改动点：

1. `GenerateFrpcConfig` 改用 `frpc.BuildClientConfig` + `frpc.BuildProxy` + `frpc.MarshalConfig`：

```go
func (a *App) GenerateFrpcConfig(serverId string) (string, error) {
	s, err := a.repo.GetServer(serverId)
	if err != nil {
		return "", fmt.Errorf("服务器不存在: %w", err)
	}
	tunnels, err := a.repo.ListTunnelsByServer(serverId)
	if err != nil {
		return "", err
	}
	common := frpc.BuildClientConfig(s.Host, s.FrpsPort, s.FrpToken)
	var proxies []v1.ProxyConfigurer
	for _, tu := range tunnels {
		if !tu.Enabled {
			continue
		}
		p, err := frpc.BuildProxy(tu.Name, tu.Protocol, tu.LocalIP, tu.LocalPort, tu.RemotePort, tu.CustomDomain, tu.Subdomain)
		if err != nil {
			return "", err
		}
		proxies = append(proxies, p)
	}
	return frpc.MarshalConfig(common, proxies)
}
```

app.go import 加 `v1 "github.com/fatedier/frp/pkg/config/v1"`。

2. `StartFrpc` 改用配置对象：

```go
func (a *App) StartFrpc(serverId string) error {
	s, err := a.repo.GetServer(serverId)
	if err != nil {
		return fmt.Errorf("服务器不存在: %w", err)
	}
	tunnels, err := a.repo.ListTunnelsByServer(serverId)
	if err != nil {
		return err
	}
	common := frpc.BuildClientConfig(s.Host, s.FrpsPort, s.FrpToken)
	var proxies []v1.ProxyConfigurer
	for _, tu := range tunnels {
		if !tu.Enabled {
			continue
		}
		p, err := frpc.BuildProxy(tu.Name, tu.Protocol, tu.LocalIP, tu.LocalPort, tu.RemotePort, tu.CustomDomain, tu.Subdomain)
		if err != nil {
			return err
		}
		proxies = append(proxies, p)
	}
	return a.frpcMgr.Start(context.Background(), serverId, common, proxies)
}
```

3. `RestartFrpc` 同理改用配置对象。

4. `StopFrpc` 不变（只传 serverID）。

5. `Init` 里 `frpc.NewManager(frpcConfigDir)` 改成 `frpc.NewManager()`（去掉 configDir 参数）。

6. `InitForTest` 同理 `frpc.NewManager()`。

- [ ] **步骤 7：更新 app_test.go**

`app_test.go` 的 `InitForTest` 签名改了（去掉 frpcConfigDir）。`TestGenerateFrpcConfig` 验证生成的 TOML 仍含 serverAddr/remotePort。其余测试基本不变。

- [ ] **步骤 8：更新 main.go**

`client/main.go` 里 `frpc.NewManager(frpcConfigDir)` 改成 `frpc.NewManager()`，删除 `frpcConfigDir` 变量。

- [ ] **步骤 9：编译 + 测试**

```bash
cd client
go build ./... && go vet ./...
go test ./... -v
```

预期：所有测试通过。frpc 内嵌后 `go build` 会拉入 frp client 包依赖，编译时间变长。

- [ ] **步骤 10：前端构建验证**

```bash
cd client/frontend
npm run build
```

预期：通过（前端无变化，但 Wails 绑定生成会重新跑，确认 App 方法签名变化未破坏前端类型）。

> 注意：`App` 的 `GenerateFrpcConfig/StartFrpc/StopFrpc/RestartFrpc/IsFrpcRunning` 对前端的方法名和返回类型**不变**，前端 api/index.ts 无需改动。Wails 会重新生成 wailsjs 绑定文件。

- [ ] **步骤 11：Commit**

```bash
git add client/
git commit -m "refactor(client): 内嵌 frpc，配置改用官方 v1 类型直接构造"
```

---

### 任务 4：端到端冒烟验证

**文件：** 无（验证步骤）

- [ ] **步骤 1：启动服务端 agent（内嵌 frps）**

修改 `server/configs/agent.local.toml` 的 `frps.config` 指向 `configs/frps.local.toml`（已存在）。启动：

```bash
cd server
go run ./cmd/agent -config configs/agent.local.toml
```

验证 health + capabilities 正常。

- [ ] **步骤 2：通过 API 启动内嵌 frps**

```bash
# 假设已加 frps Start/Stop API（本计划未加，需手动验证）
# 或在 main.go 临时加 go frpsMgr.Start(context.Background())
```

> 注意：本计划未给 agent 加 frps Start/Stop 的 HTTP 端点（设计文档归入 v0.3）。冒烟验证内嵌 frps 能启动：临时在 main.go 加 `go frpsMgr.Start(context.Background())`，启动后 `curl 127.0.0.1:7000` 应能连上（frps bindPort 监听）。验证后**删除临时行**。

- [ ] **步骤 3：启动客户端 wails dev**

```bash
cd client
wails dev
```

GUI 窗口打开后：
1. 添加服务器（host=127.0.0.1, frps_port=7000, token=your-frp-token, agent_url=http://127.0.0.1:7400, agent_token=change-me-agent-token）
2. 检测能力 → 应返回 capabilities
3. 创建 TCP 映射 → 启动 frpc → 日志页看到 frpc 连接 frps 的日志（因为 frps 现在内嵌运行，frpc 能真正连上！）
4. 创建 HTTP 映射 → 启动 frpc → 应能通过域名访问

> 这是 v0.1 以来首次能真正端到端跑通——因为 frps 内嵌在 agent 里，frpc 内嵌在客户端里，两者能真正建立隧道。

- [ ] **步骤 4：最终全量验证**

```bash
cd server && go test ./... && go build ./...
cd client && go test ./... && go build ./...
cd client/frontend && npm run build
```

- [ ] **步骤 5：清理临时改动 + Commit**

删除 main.go 临时加的 `go frpsMgr.Start` 行（如果有）。清理冒烟产生的 data/server.local.db 等。

```bash
git add -A
git commit -m "chore: 内嵌 frp 端到端冒烟验证通过"
```

---

## 自检结果

**1. 规格覆盖度：**
- 决策 1.C：客户端内嵌 frpc ✅ 任务 3；服务端 agent 内嵌 frps ✅ 任务 2
- 决策 3.B：直接构造 v1 配置对象 ✅ 任务 2/3 用 `v1.ClientCommonConfig`/`v1.ServerConfig`/`v1.*ProxyConfig`
- 删除自写配置结构 ✅ frpsc 包删除、frpc.Config/Auth/Proxy 删除
- frps Start/Stop/Restart 真正实现 ✅ 任务 2 manager.go
- 前端 + 业务逻辑不变 ✅ App 方法对外签名不变

**2. 占位符扫描**：无 TODO；每步有完整代码。任务 4 步骤 2 的 frps 启动验证给了明确的临时改动方式。

**3. 类型一致性：**
- `v1.ServerConfig`（BindPort/VhostHTTPPort/VhostHTTPSPort/SubDomainHost/Auth/AllowPorts）在 api/portpool/frps 三处使用一致
- `v1.ClientCommonConfig`（ServerAddr/ServerPort/Auth.Token）在 frpc.BuildClientConfig 和 app.go 使用一致
- `v1.ProxyConfigurer` 接口 + 各 `*ProxyConfig` 在 BuildProxy 和 Manager.Start 使用一致
- `frps.Manager.Config()` 返回 `*v1.ServerConfig`，api.capabilities 和 portpool 都用这个类型
- `frpc.Manager` 接口 `Start(ctx, serverID, *v1.ClientCommonConfig, []v1.ProxyConfigurer)` 在 manager.go 实现、app.go 调用一致
- `frpc.NewManager()` 无参，main.go 和 app.go Init 调用一致

**已识别的风险：**
1. **frp 依赖体积大**：yamux/quic/kcp 等会让 go.mod 变重、首次编译变慢（几分钟）。任务 1 已提示。
2. **frpc Service.Run login 失败行为**：任务 3 步骤 5 测试用不可达端口，LoginFailExit 默认 true 会让 Run 返回 error，goroutine 退出，IsRunning 可能立即变 false。测试设计为容忍这种情况（不 panic 即可）。若想 frpc 持续重试，在 BuildClientConfig 设 `common.LoginFailExit = lo.ToPtr(false)`——但 v0.1 先用默认。
3. **frps 内嵌端口冲突**：任务 2 Start 后 frps 占用 bindPort=7000，若机器上已有进程占用会失败。冒烟前确认 7000 空闲。
4. **日志集成**：frp 用自己的 `pkg/util/log`，内嵌后其日志默认 stdout/stderr。客户端 Wails 事件转发需把 frp log 重定向——任务 3 的 `SetLogCallback` 在内嵌后实际无法直接捕获 frp log（因为 frp 用全局 logger 而非返回 io.Reader）。**v0.1 暂不深度集成日志**，frp log 走默认输出；若需转发，后续用 `log.InitLogger` 重定向到自定义 writer。本计划不阻塞，日志功能降级为"frp log 走 stdout，Wails 控制台可见"。
5. **TOML 序列化**：`MarshalConfig` 用 json→toml 转换，v1 类型的 json tag 是 camelCase，toml 也用 camelCase，字段名一致。但嵌套结构（如 ProxyBackend 内嵌在 ProxyBaseConfig）json 扁平化，toml 也扁平化，应一致。若序列化结果与手写 TOML 格式有差异，不影响功能（只是"查看配置"展示用）。

这些都有处理方式，不阻塞。
